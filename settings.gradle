pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'order'

include 'order-api'
include 'order-core'
include 'order-rabbit-consumer'

